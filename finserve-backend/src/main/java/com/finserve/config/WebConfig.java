package com.finserve.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebConfig is intentionally empty — interceptor and CORS registration
 * have been consolidated into CorsConfig to avoid duplicate WebMvcConfigurer beans.
 */
@Configuration
public class WebConfig {
}
